import React from "react";
import ContainerBlock from "../components/ContainerBlock";

const projects = () => {
  return (
    <ContainerBlock>
      <div>Projects</div>
    </ContainerBlock>
  );
};

export default projects;
